# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ---- Settings / validation ----
pydantic==2.10.5
pydantic-settings==2.7.1
email-validator==2.2.0
python-slugify==8.0.4

# ---- Database ----
SQLAlchemy[asyncio]==2.0.37
asyncpg==0.30.0
psycopg[binary]==3.2.4
alembic==1.14.1
greenlet==3.1.1

# ---- Auth (JWT JOSE) ----
python-jose[cryptography]==3.3.0
argon2-cffi==23.1.0
bcrypt==4.2.1

# ---- Cache / queue ----
redis==5.2.1
celery[redis]==5.4.0

# Rate limiting uchun qo'shimcha paket kerak emas —
# `app/core/rate_limit.py` Redis'dagi fixed-window hisoblagichdan foydalanadi.

# ---- HTTP client (integratsiyalar) ----
httpx==0.28.1
tenacity==9.0.0

# ---- Object storage (MinIO / S3) ----
boto3==1.36.6

# ---- Sertifikat (PDF + QR) ----
fpdf2==2.8.2
qrcode[pil]==8.0
Pillow==11.1.0
